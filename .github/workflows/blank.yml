name: Hello-Abhisek

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "Hello Abhisek, GitHub Actions Working Successfully"
